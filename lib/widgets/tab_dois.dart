import 'package:flutter/material.dart';

class TabDois extends StatelessWidget {
  const TabDois({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Tab(
      text: "Conversas",
    );
  }
}
