import 'package:flutter/material.dart';

class TabQuatro extends StatelessWidget {
  const TabQuatro({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Tab(
      text: "Chamadas",
    );
  }
}
